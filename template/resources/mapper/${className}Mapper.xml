<#include "/macro.include"/>
<#assign className = table.className>   
<#assign classNameFirstLower = table.classNameFirstLower>   
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<#macro mapperEl value>${r"#{"}${value}}</#macro>
<mapper namespace="${basepackage}.dao.${className}Dao">

	<select id="pageFind${className}"
			parameterType="${basepackage}.domain.vo.request.${className}ReqVO"
			resultType="${basepackage}.domain.vo.response.${className}VO">
	
	</select>


	<select id="find${className}ListByVo"
			parameterType="${basepackage}.domain.vo.request.${className}ReqVO"
			resultType="${basepackage}.domain.vo.response.${className}VO">

	</select>
	
</mapper>